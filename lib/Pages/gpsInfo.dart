import 'package:flutter/material.dart';

class GPSinfo extends StatelessWidget {
  const GPSinfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
