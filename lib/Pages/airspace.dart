import 'package:flutter/material.dart';

class Airspace extends StatelessWidget {
  const Airspace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
