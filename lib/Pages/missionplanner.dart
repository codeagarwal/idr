import 'package:flutter/material.dart';

class MissionPlanner extends StatelessWidget {
  const MissionPlanner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
